[package]
name = "generate-example-images"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
image = { version = "0.25.1", default-features = false, features = ["jpeg"] }
noise-functions = { path = ".." }
